# v5.8: 同期復旧・遠征一覧とおすすめの改善

## 動作

- クラウド読込成功時に自動保存の準備を復帰。失敗は成功表示にせず、再試行ボタン・online／focus／visibilitychangeで再試行する。
- 未同期の設定・記録は先に保存する。端末内の保存処理は順番に実行し、遅い古い保存結果が新しい状態を成功扱いにしない。
- 未同期フラグはユーザーごとに端末へ保持。再読込後も未保存の変更を送信できる。別ユーザーの古い応答は適用しない。
- 保存応答の履歴・資源推移は最新の端末内記録にマージする。
- 同期中／同期済み／未同期／失敗／オフラインと最終成功時刻を表示。
- 遠征一覧は検索が先頭。お気に入り3件、並べ替えは専用モード。詳細から戻ると検索と一覧内スクロールを維持。
- お気に入りにも交戦リスクを表示。タグと時給は任意表示。
- おすすめは3件＋展開、報酬・前提込み時間効率・損傷回避で比較。未記録の月次前提を再帰的にたどり、共通の前提は時間に二重計上しない。
- II型除外は候補本体と前提ルートを評価。完了済み月次前提は除く。通常遠征の初回クリア状態は管理していないため、通常の初回ルートも含む慎重な判定。
- 改修資材優先に改修資材のない遠征を補充しない。アイテムは最大個数を使用し、期待値と区別。
- 2個前の前提を直接の前提ごとにまとめる。3個前より先は折りたたみ。
- 1分前は手動記録可能のまま。通知の配信方式は変更せず、設定に約5分間隔と遅延の目安を明示。

## 検証

- `npm test`: 12件。深い前提のII型除外・達成済み前提・合流の所要時間・改修資材フィルタを追加。
- `npm run build`: 成功。既存の500kB超チャンク警告は残る。
- スマホ幅の操作: 検索、詳細、一覧への復帰、前提、月次記録、各おすすめ条件、野埼の再開始・リセット、残り1分以内の手動帰投記録。
- 320/390/768/1440pxで一覧スクロール・最終行到達・横はみ出しなし。CDPタッチ操作でも一覧をスクロール。
- `tests/browser-sync.mjs`: 実アカウントを使わないブラウザ回帰テスト。初回読込失敗→手動読込→自動保存復帰、保存失敗表示→通信復旧、未同期のまま再読込→保存復旧を確認。

ブラウザ回帰テストは別途PlaywrightとChromiumがある環境で `node tests/browser-sync.mjs` を実行する。標準の `playwright` モジュールを利用する。外部にインストール済みの場合は `PLAYWRIGHT_MODULE` にモジュールパス、`CHROMIUM_PATH` に実行ファイル、必要なら `CHROMIUM_ARGS` に引数のJSON配列を指定する。モックの認証・REST通信だけを使用し、本番データや通知は変更しない。

## 制約

- おすすめの比較点は独自の重み付け。アイテムの最大個数であり、確率や大成功率を含む期待値ではない。
- 時間効率は目標遠征の比較点÷未記録の月次前提込み時間。前提報酬・消費・修理費・初回開放の時間は含まない。
- この改善は設定・履歴スナップショットの復旧。複数端末で同じ設定を同時編集した場合のフィールド単位競合解決は未実装。実機Android・実アカウント複数端末・本番通知は未検証。
