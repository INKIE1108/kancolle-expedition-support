# UI v5.6 修正内容

v5.5 の落ち着いた配色を維持しつつ、スクリーンショットで確認できた細かなレイアウト崩れと情報設計を修正した版です。

## 主な修正

- `RESOURCE STOCK / 所持資源の推移` の左余白とセクション整列を修正
- `TARGET PLANNER / 資源目標プランナー` の左見切れを修正
- 所持資源グラフの左右端に 3% の安全域を追加し、最初・最後の点と日付の見切れを防止
- SVG 内の円を廃止し、HTML の丸いポイントマーカーを重ねる方式へ変更。横長に潰れない真円表示に修正
- グラフ線に `vector-effect: non-scaling-stroke` を追加し、画面比率による線幅の変形を抑制
- グラフ切替を「表示する資源」と「表示期間」の2グループに分離
- 資源目標入力を「現在資源を入力」と同じコンパクトな2×2レイアウトに統一
- 目標達成済みの場合の `残り -xxxxx` を `目標より +xxxxx` 表示へ変更
- タイマー画面内の重複した `TIMERS / タイマー` 見出しを削除
- 設定画面内の重複した `SETTINGS / 設定 / 通知・同期・外観` 見出しを削除
- 設定画面を Hub → 個別設定のサブビュー方式へ変更し、同じ項目が上下に二重表示される構造を解消
- 「アカウント・同期」メニューに対応する実際のログイン・クラウド同期画面を追加
- 遠征詳細の前提条件・成功条件・編成条件は、外側の水色面を抑えてニュートラルな階層に整理
- ピン留め遠征の並び替えボタンをより控えめなユーティリティ表示に調整
- PWA キャッシュ名・表示バージョンを v5.6.0 に更新

## CSS構成

`src/style.css` は既存機能互換用のレガシースタイルです。
現在の最終UIは `src/ui-v56.css` を最後に読み込み、ここで表示を確定させています。

## 確認

- `npx tsc -b` : 成功
- `ui-v56.css` : tinycss2 で構文エラー 0 件

この実行環境では、アップロード元の `node_modules` が Windows 用バイナリを含むため Linux 上の Vite/Rollup 本番ビルドは実行できませんでした。Windows 側では通常どおり `npm install` → `npm run build` で確認してください。
