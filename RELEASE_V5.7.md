# v5.7.0

## 利用者向けの変更

- 開放ルートの直前・2段階前を優先表示。全前提は折りたたみから確認。
- マンスリーおすすめ（資源・アイテム）と前提／後続のリンクを追加。
- 🔴 交戦II型・大破しやすい、🟠 交戦I型・損傷あり、🟢 交戦なしを表示。
- 編成条件の選択肢、旗艦、必須枠と自由枠を区別。具体例では自由枠を駆逐で補う。
- 野埼の古いクラウド完了状態による上書きを防止。通信失敗でも直前の同期時刻を維持。
- 残り1分の手動帰投記録を維持。自動終了は行わない。
- Discord設定なしで遠征Push通知を予約可能。
- 遠征変更・プリセット適用・通知OFFで古い予約を取り消す。
- 15日正午でマンスリー記録期間を切り替える。

## 通知・同期

- 条件付き更新で通知予約を取得し、並行処理の二重取得を防止。
- 成功した送信先を保存し、失敗先だけを最大5回まで再試行。
- 配信中にプロセスが停止した場合は10分のリース切れ後に再処理。
- 通知の外部配信とDB保存は一つのトランザクションにできないため、送信直後の
  プロセス停止では重複の可能性が残る。Pushには予約ごとの同じtagを使う。
- ネットワーク送信にタイムアウトを設定。共通Webhook中継APIは410で閉鎖。
- Webhook/Pushの送信先ホストを制限し、Discordメンション展開を無効化。
- クラウド保存は読み込み失敗時に上書きせず、条件付き保存で競合時に再マージ。
- 古いerror通知の一括再送は行わない。今後の通知予約が再送の対象。
- GitHub Actionsは5分間隔のまま。実行遅延があるため秒単位の通知時刻は保証しない。

## DB更新

既存環境には `supabase/schema.sql` の末尾 `-- v5.7:` 以降を適用する。
カラム追加とuser_settingsのRealtime配信追加のみで、既存データを削除しない。
今回の対象Supabaseプロジェクトへ適用・カラムとRLSの確認済み。
ソースの反映はPRのマージ後。古いタブも更新してから使う。

## 確認

- `npm test`: 野埼の競合、15日正午・年越し、開放ルート、編成例、マンスリー候補、
  送信先制限、部分再送、配信APIの同時実行を検証。
- `npm run build`: TypeScriptとViteの本番ビルド。
- ブラウザー: 390px幅のルート表示、マンスリー、損傷フィルター、野埼の再計測、
  1分前の手動帰投記録を確認する。
- 実利用者へのDiscord/Pushテスト通知は送信していない。

既存DBのSecurity Advisorには rls_auto_enable の公開実行権限と漏えいパスワード保護の
警告が残っている。今回の追加カラム・Realtime設定による新しい権限付与はない。
参考: https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable
