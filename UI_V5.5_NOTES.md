# UI v5.5 修正メモ

## 今回の主な修正

- 「今日の獲得資材」の青ベタ背景を撤去し、ニュートラルな面＋資源色ドットに変更
- 「よく使う遠征」の青ベタ背景・青い上下ボタンを撤去
- 選択中の遠征は淡いブルーグレー＋左アクセント線だけに変更
- 「現在資源を入力」の入力欄を根本修正
  - 旧 `width: 46% !important` の影響を排除
  - 6桁数値が見切れない `width: 100% / min-width: 0` 構成
  - 数値スピナー非表示
  - 2列×2行のコンパクトな入力行（スマホは1列）
  - 資源ドットが入力欄内へ重複生成される旧CSSを無効化
- 遠征詳細の大きな水色面を撤去し、白・薄グレー・セパレータ中心に変更
- 記録画面の青いカード群、青い履歴行、紫がかった選択背景をニュートラル化
- Liquid Glass はナビゲーション・セグメント切替に限定
- コンテンツカードはSolid Surfaceへ戻し、ガラス表現の多用を停止
- アクセントカラーを低彩度ブルーグレーに変更
- お気に入りボタンを青からアンバー系の控えめな色へ変更
- タブアイコンを文字記号から統一SVGへ変更
- `一覧・検索 / おすすめ・マンスリー` の外枠を見やすく調整
- Light / Dark 両テーマを同じ階層ルールに統一

## CSS構成

`src/style.css` は既存機能・レイアウト互換のベースとして残し、
最終デザインは `src/ui-v55.css` を `style.css` の後に読み込む構成に変更。

また、過去に青ベタ表示を復活させていた高詳細度の汎用ボタンルールを
`style.css` から削除している。

## 検証

- TypeScript: `npx tsc -b` 成功
- CSS: tinycss2で `style.css` / `ui-v55.css` とも構文エラー 0
- カスケード監査で以下の最終適用ルールを確認
  - 今日の獲得資材 → `var(--ui-surface-muted)`
  - よく使う遠征選択行 → `var(--ui-surface-selected)`
  - 上下ボタン → `transparent`
  - お気に入り → 控えめなアンバー
  - 現在資源 input width → `100%`
  - 現在資源 input padding → `0 12px`
  - 遠征詳細の大面積背景 → `transparent`
  - 履歴行 → `transparent`

## バージョン

- App: 5.5.0
- Service Worker cache: `kancolle-expedition-support-v5.5.0`
